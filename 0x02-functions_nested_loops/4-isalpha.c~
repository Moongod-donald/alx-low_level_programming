#include "main.h"

/**
 * isalpha - checks if char is a letter, lowercase or uppercase
 *
 * Return: 1 (if value is a letter), 0 (if otherwise)
 */
int _isalpha(int c)
{
	if ((c >= 97 && c <= 122) || (c >= 65 && c <= 90))
		return (1);
	else
		return (0);
}
