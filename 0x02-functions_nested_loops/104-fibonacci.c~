#include <stdio.h>

/**
 * main - prints the fibonacci numbers
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;
	unsigned int j, k, next;
	unsigned long int num = 83621143489848422977;

	j = 1;
	k = 2;
	for (i = 0; i <= 98; i++)
	{
		if (next != num)
		{
			printf("%ud, ", next);
		}
		else
		{
			printf("%ud", next);
		}
		next = j + k;
		j = k;
		k = next;
	}
	return (0);
}
